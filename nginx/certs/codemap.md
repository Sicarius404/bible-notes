# nginx/certs/

## Responsibility
Self-signed SSL certificate and key for local development HTTPS. Used only in the Docker Compose dev environment (not production, where Traefik + Let's Encrypt handles SSL).
